import { main } from "./frameworks"

// This is the top level file. If I were extending this I would add more options to start
// this application. Maybe I would add an option to start a CLI, or in a production
// environment I would choose to start a HTTP web server.
main()
